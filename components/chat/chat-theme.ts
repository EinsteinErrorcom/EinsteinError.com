/** Single gold used across MAX-LIT chat UI — must match .max-lit-chatbox-page-title (#d0ab47). */
export const MAX_LIT_GOLD = '#d0ab47';

export const maxLitGoldStyle = {
  color: MAX_LIT_GOLD,
  borderColor: MAX_LIT_GOLD,
} as const;
